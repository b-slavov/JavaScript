### Here is another code block, working on every browser!

```javascript
$this.on('mouseup keyup', function (e) {
	var selectionStart = this.selectionStart;
	var selectionEnd = this.selectionEnd;
	var selection = selectionEnd - selectionStart;
});
```